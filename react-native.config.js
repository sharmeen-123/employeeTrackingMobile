module.exports = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./assets/fonts/yahei.tff'], // This assumes that your font files are in a directory named `fonts` inside an `assets` directory at the root of your project
};
